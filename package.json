{
  "name": "bridge-finance-landing",
  "version": "1.0.0",
  "private": true,
  "description": "William Zhu — Bridge Finance mortgage broker landing page",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
